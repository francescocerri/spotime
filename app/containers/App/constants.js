const scope = 'app/App';

export const SET_GLOBAL_MESSAGE = `${scope}/SET_GLOBAL_MESSAGE`;
export const RESET_GLOBAL_MESSAGE = `${scope}/RESET_GLOBAL_MESSAGE`;

export const TOKEN_REQUESTED = `${scope}/TOKEN_REQUESTED`;
export const TOKEN_SUCCEEDED = `${scope}/TOKEN_SUCCEEDED`;
export const TOKEN_FAILED = `${scope}/TOKEN_FAILED`;

export const REFRESH_TOKEN_REQUESTED = `${scope}/REFRESH_TOKEN_REQUESTED`;
export const REFRESH_TOKEN_SUCCEEDED = `${scope}/REFRESH_TOKEN_SUCCEEDED`;
export const REFRESH_TOKEN_FAILED = `${scope}/REFRESH_TOKEN_FAILED`;
