/*
 *
 * HomePage constants
 *
 */
const scope = 'app/Homepage';

export const DEFAULT_ACTION = `${scope}/DEFAULT_ACTION`;

export const RECOMMENDATIONS_REQUESTED = `${scope}/RECOMMENDATIONS_REQUESTED`;
export const RECOMMENDATIONS_SUCCEEDED = `${scope}/RECOMMENDATIONS_SUCCEEDED`;
export const RECOMMENDATIONS_FAILED = `${scope}/RECOMMENDATIONS_FAILED`;
